import React from 'react';
import './Slider.scss';

const Slider = () => {
    return (
        <section className="Slider">
            <a className="Slider__image"></a>
        </section>
    );
};

export default Slider;